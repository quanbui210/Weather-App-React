const Wrapper = (props) => {
    return props.children
    //the children props holds all the content you're passing between the opening and closing tag of ur custom component
}

export default Wrapper